import queuer
# TODO: add __init__.py file to botlogistics/tracking and .
from tracking import fleet

class Planner(object):
    def __init__(self):
       self.queue = querer.FleetQueue()
       self.fleet = Fleet.info()
